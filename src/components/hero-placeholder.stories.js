// @flow

import * as React from 'react';
import {storiesOf} from '@storybook/react-native';

import HeroPlaceholder from './hero-placeholder';

storiesOf('HeroPlaceholder', module).add('Default', () => <HeroPlaceholder />);
